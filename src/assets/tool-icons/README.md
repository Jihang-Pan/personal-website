# Tool icons

Most brand SVGs in this directory come from the Simple Icons npm package.
Source: https://simpleicons.org/

The Feishu icon uses the official app artwork published by Feishu and is stored as
an inline data URI in `feishu.ts`.

All icons are bundled and inlined at build time on the `/uses` page.
