---
type Props = {
  class?: string;
  wide?: boolean;
};

const { class: className, wide = false } = Astro.props;
---

<section
  class:list={[
    "px-4 sm:px-6 mx-auto w-full",
    wide ? "site-shell" : "max-w-2xl",
    className,
  ]}
>
  <slot />
</section>
